{
  "name": "apishark-frontend",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "node ./build.mjs",
    "dev": "node ./build.mjs --watch",
    "test": "node ./node_modules/typescript/lib/tsc.js --noEmit -p tsconfig.json && npm run build"
  },
  "devDependencies": {
    "esbuild": "^0.27.3",
    "typescript": "^5.9.3"
  }
}
